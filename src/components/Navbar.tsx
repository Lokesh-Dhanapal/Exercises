import { AppBar,Toolbar, Typography } from "@mui/material";

const Navbar = () => {
  return (
    <AppBar color="info">
        <Toolbar>
            <Typography>
                POKEMON
            </Typography>
        </Toolbar>
    </AppBar>
  )
}

export default Navbar